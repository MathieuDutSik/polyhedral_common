#include "list.h"
#include "StabChain.h"

int main(int argc, char *argv[])
{
  try {
    
  }
  catch (TerminalException const& e) {
    exit(e.eVal);
  }
  std::cerr << "Completion of the program\n";
}
